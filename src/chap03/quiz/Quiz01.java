package chap03.quiz;

public class Quiz01 {
    
    // 람다 문법에 맞지 않는 람다 표현식은?
    // 1) () -> {}
    // 2) () -> "Raoul"
    // 3) () -> {return "Mario"}
    // 4) (Integer i) -> return "Alan" + i;
    // 5) (String s) -> {"Iron Man";}
    
    // result -> 4, 5
    // 4) 중괄호를 사용하지 않으면 return 이 작성된 것과 동일하게 작동
    // 5) 중괄호를 사용했으므로 return 을 작성해야함
    
}
