package chap04.quiz;

public class Quiz02 {
    
    // 다음 스트림 파이프라인에서 중간 연산과 최종 연산을 구별
//    long count = menu.stream()
//            .filter(d -> d.getCalories() > 300) -> 중간 연산
//            .distinct() -> 중간 연산
//            .limit(3) -> 중간 연산
//            .count(); -> 최종 연산
    
}
