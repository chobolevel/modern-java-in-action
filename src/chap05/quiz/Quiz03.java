package chap05.quiz;

public class Quiz03 {

    // map과 reduce를 이용해 스트림의 요리 개수 계산

    // 두 가지 result

//    int count = menu.stream()
//            .map(d -> 1)
//            .reduce(0, (a, b) -> (a + b));

//    long count = menu.stream().count();

}
